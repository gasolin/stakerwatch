import React, {useEffect, useState} from 'react';

export const useCoingeckoPrices = (addresses, fetch) => {
  const [tokens, setTokens] = useState({});



  return [tokens]
}

export default useCoingeckoPrices;
